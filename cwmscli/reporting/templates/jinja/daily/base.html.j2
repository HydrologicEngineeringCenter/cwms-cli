<!-- templates/base.html.j2 -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{% block title %}{{office}} — {{report.name}}{% endblock %}</title>
  <style>
    #daily-report .logo-table th { border: none !important; vertical-align: middle; width: 100%; margin: 0 auto; }
    #daily-report .logo-table img { display: block; }
    #daily-report .table-responsive { overflow-x: auto; }
    #daily-report .data-table thead tr:nth-child(1) th { background: #6C8578; color: #fff; position: sticky; top: 0; z-index: 3; font-size: small; height: 2.5rem; }
    .data-table thead tr:nth-child(2) th { background: #C1CCC4; color: #333; position: sticky; top: 2.5rem; z-index: 2; font-size: small; height: 1.6rem; }
    .data-table thead tr:nth-child(3) th { background: #C1CCC4; color: #333; position: sticky; top: calc(2.5rem + 1.6rem); z-index: 1; font-size: small; }
    table { border-collapse: collapse; width: 100%; }
    .data-table th, .data-table td { border: 1px solid #444; padding: 4px; white-space: nowrap; font-size: small; text-align: center; }
    #daily-report .data-table tbody tr.table-secondary { background-color: #f2f2f2; }
    #daily-report .data-table tbody tr:hover { background-color: rgba(0,0,0,.05); }
    @media (max-width: 600px) {
      #daily-report .data-table th, #daily-report .data-table td { white-space: normal; font-size: x-small; padding: .25rem .5rem; }
      #daily-report .logo-table th { width: 100% !important; display: block; text-align: center; }
      #daily-report .logo-table img { margin: 0 auto; }
    }
  </style>
  {% block head %}{% endblock %}
</head>
<body>
<div id="daily-report">
  {% block body %}{% endblock %}
</div>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
</body>
</html>
